package org.jewelheart.admin

import androidx.compose.ui.graphics.Color

/** Volunteer home / mockups.docx — keep in sync with iOS VolunteerHomeColors and server SDUI hex. */
object JewelHeartColors {
    val Gold = Color(0xFFFFCA10)
    val SummaryBlue = Color(0xFF7A95CA)
    val ActionMaroon = Color(0xFF92160E)
    val DemoNoteGray = Color(0xFF666666)
    val ErrorRed = Color(0xFFCC0000)
}
